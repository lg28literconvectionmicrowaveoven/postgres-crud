Simple Postgres auth CRUD application

Set up by running: -

```
pip install -r requirements.txt
cd frontend
npm i
```

Run frontend:

```
cd frontend
npm run dev
```

Run backend:

```
fastapi run main.py
```
